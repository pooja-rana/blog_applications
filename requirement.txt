# **Django Rest Framework** is used for REST API
# See https://www.django-rest-framework.org/
djangorestframework==3.15.2

# **psycopg2-binary**
# See https://www.psyc
psycopg2-binary==2.9.3

# **environs** is used for read the environs variable.
# See https://pypi.org/project/environs/
environs==11.0.0

# **Django** is use for webserver
# See https://www.djangoproject.com/start/
Django==4.2.14


# **Pillow** is use for image
# See https://pypi.org/project/pillow/
Pillow==11.1.0


# **django-cors-headers** is used for allows your resources to be accessed on other domains.
# see https://pypi.org/project/django-cors-headers/
django-cors-headers==3.13.0


# ** simplejwt used to token authentica
# see https://github.com/jazzband/djang
djangorestframework-simplejwt==5.4.0


# **pre-commit** is used to for identifying simple issues before submission to code review
# See https://pre-commit.com/
pre-commit==2.20.0


# **social-auth-app-django** is used to for login user through the social media
# See https://pypi.org/project/social-auth-app-django/
social-auth-app-django==5.4.3


# **django-allauth** is used to for  3rd party (social) account authentication.
# See https://pypi.org/project/django-allauth/
django-allauth==64.2.1
